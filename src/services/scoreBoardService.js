export function sendScoreToClassification (localScore, visitorScore) {
  console.log('The score are sended to classification')
  console.log(`The current score are: Local ${localScore} - Visitor ${visitorScore}`)
}
